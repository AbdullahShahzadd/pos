const Admin = require("../models/admin")

module.exports.employee = async function(inFname, inLname, inEmail, id){
	// var changedEmp = await Admin.findOneAndUpdate({_id: id}, {fname: inFname, lname: inLname, email: inEmail})

	// var employees = await Admin.find({locations: changedEmp.locations})
}
